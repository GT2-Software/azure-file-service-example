apiVersion: v1
kind: Service
metadata:
  name: api-service
spec:
  selector:
    app: file-service
  ports:
    - port: 5000
      protocol: TCP
      targetPort: 8080
      #nodePort: 30102
  type: ClusterIP
  #externalIPs:
  #  - 192.168.0.63
  #  - 192.168.0.200
